Доброго времени суток!

Старался творчески подойти к заданиям из источников (learn.javascript.ru),
обЪединяя или изменяя их (в сторону усложнения), мне кажется так нагляднее
самостоятельность работы.

Все материалы по темам разделил по папкам, в js файлах по несколько заданий,
все кроме последнего закомменчены (комментарии к заданиям - отдельно как строки,
сами задания как блоки), поскольку делал их поочередно, комментил предыдущие чтобы не мешали.
Надеюсь это не доставит особых проблем при проверке.

На мой взгляд самостоятельная работа по темам: Базовые операторы,
Логические операторы, Операторы сравнения, Таблица приоритетов, Типы данных,
Преобразование типов(кроме преобразования обЪектов в примитивы, но я так понимаю
это будет дальше), достаточно полно отражена в представленных материалах
по другим темам, поэтому выделять их в отдельные папки не стал.
В принципе можно было и "Условное ветвление" не выделять, но у меня остался
этот пример (сделал и не удалил пока изучал learn.javascript.ru на стадии подготовки).
Что касается темы "Console" (материал прочитал несколько раз) то я регулярно использую console.log(),
пoнял (наверное)) как использовать console.group(), console.info() - практически то же самое,
что и console.log(), только для информационных сообщений (хотя по сути они все информационные... или нет?). А что касается console.warn() и console.error() - ума не приложу, отладчик браузера info, warn и error
выводит автоматически, а чтобы использовать их самому, надо знать (или хотя бы предполагать)
где будет предупреждение или ошибка (а если знаешь то почему не исправить?) или не так?
В общем - это мой главный вопрос на 9 число.

Вопрос из 6 задания "дата и время"
//Почему
let today = new Date();
today.setHours(0, 0, 0, 0);
console.log(today); // Sun Nov 07 2021 00:00:00 GMT+0200
// а
let today = new Date().setHours(0, 0, 0, 0);
console.log(today); // 1636236000000
// и
let today = new Date();
let now = today.setHours(0, 0, 0, 0);
console.log(now); // 1636236000000
//???
